#!/usr/bin/env python3
"""Bootstrap, ignore, and start a workshop environment.

Thin entrypoint; all logic lives in the my_workshop package so it can be
imported and unit-tested. See `my_workshop` for details, or run with --help.
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from my_workshop.cli import main

if __name__ == "__main__":
    main()
